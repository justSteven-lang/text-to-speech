apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tts-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: tts-app
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
