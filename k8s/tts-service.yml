apiVersion: v1
kind: Service
metadata:
  name: tts-service
  labels:
    app: tts-app

spec:
  selector:
    app: tts-app

  ports:
    - port: 80
      targetPort: 8080
      name: http

  type: ClusterIP
